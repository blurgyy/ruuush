#include<cstdio>
#include<algorithm>
#define ll long long
using namespace std;

ll x, y, l, r;


int main()
{
    while(~scanf("%lld%lld", &x, &y))
    {
        if(x == y && x == 1)
        {
            if(l == 1)printf("1\n");
            else printf("0\n");
        }
        else
        {

        }
    }
    return 0;
}
