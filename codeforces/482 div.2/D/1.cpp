#include <bits/stdc++.h>
using namespace std;
const int maxn = 100010;

int x, k, s;

int main(){
    while(~scanf("%d", &q)){
        while(q --){
            scanf("%d", op);
            if(op == 1){
                int ins;
                scanf("%d", &ins);

            }
            else {
                scanf("%d%d%d", &x, &k, &s);
                
            }
        }
    }
    return 0;
}