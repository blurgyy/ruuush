#include <bits/stdc++.h>
using namespace std;
const int inf = 0x3f3f3f3f;

int n, k, x;

int main(){
    while(~scanf("%d%d%d", &n, &k, &x)){
        for(int i = 0; i < n; ++ i){
            scanf("%d", a + i);
        }
        
    }
    return 0;
}