#include<bits/stdc++.h>
using namespace std;

int T;

int main(){
    while(~scanf("%d", &T)){
        while(T--){
            scanf("%d%d", &n, &m);
            for(int i = 1; i <= m; ++ i){
                
            }
        }
    }
}