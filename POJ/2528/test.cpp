#include<bits/stdc++.h>
#define maxn 100010
using namespace std;

int n, a[maxn];

int main()
{
    int a[maxn] = {0};
    printf("a[0] = %d\n", a[0]);
    memset(a, 0x7fffffff, sizeof(a));
    printf("a[0] = %d\n", a[0]);
    return 0;
}
