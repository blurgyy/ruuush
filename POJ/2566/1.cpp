#include<bits/stdc++.h>
using namespace std;
const int maxn = 100010;

int n, k, q;
int a[maxn];

int main(){
    while(~scanf("%d%d", &n, &k)){
        for(int i = 0; i < n; ++ i)
            scanf("%d", a + i);
        while(k--){
            scanf("%d", &q);
            int front = 0, rear = 1;
            int sum = a[0];
            
        }
    }
    return 0;
}