#include<bits/stdc++.h>
using namespace std;
const int maxn = 50;
const int dx[] = {-1, 0, 1, 0};
const int dy[] = { 0, 1, 0, -1};

int T, n;
int acc[maxn][maxn];
char mp[maxn][maxn];

bool dfs()

int main(){
    scanf("%d", &T);
    while(T --){
        scanf("%d", &n);
        for(int i = 1; i < n; ++ i)
            scanf("%s", mp[i] + 1);

    }
}
