#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int maxn = 100010;

int T, n, k;
int x[maxn];

int main(){
    scanf("%d", &T);
    while(T--){
        scanf("%d%d", &n, &k);
        for(int i = 0; i < n; ++ i){
            scanf("%d", x + i);

        }
    }
}