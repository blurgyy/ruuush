#include<cstdio>
#include<cstring>
#define maxn 10010

int n, m;
float p[maxn][maxn];
int cost[maxn];
float posb[maxn];

void init()
{
    memset(p, 0, sizeof(p));
    memset(cost, 0, sizeof(cost));
    memset(posb, 0, sizeof(posb));
}

int main()
{
    while(~scanf("%d%d", &n, &m))
    {
        for(int i = 0; i < n; i++)
            scanf("%d%f", cost + i, posb + i);
        for(int i = 0; i < n; i++)
        {
            for(int j = )
        }
    }
}
