#include<cstdio>
#include<vector>
#include<map>
using namespace std;

int main()
{
    int Size;
    vector a;
    while(~scanf("%d", Size))
    {
        a.clear();
        int sum = 0;
        for(int i = 0; i < Size; i++)
        {
            scanf("%d", tmp);
            a.push_back(tmp);
            sum += tmp;
        }
        int t = 1;
        int time = Size;
        while(Size--)
            t *= 2;
        for(int i = 0; i < t; i++)
        {
            int tmp = i;

        }
    }

    return 0;
}
0 1 0 0 0 1 2 1 0 0 0 1 1
