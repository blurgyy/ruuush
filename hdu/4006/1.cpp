#include<bits/stdc++.h>
using namespace std;
const int maxn = 100010;

int main(){
    while(~scanf("%d%d", &n, &k)){
        while(n --) {
            scanf("%s", op);
            if(!strcmp(op, "I")){
                
            }
            else {
                printf("%d\n", query());
            }
        }
    }
    return 0;
}