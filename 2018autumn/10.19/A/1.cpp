#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
const int maxn = 1000100;

int n;
ll a[maxn];
ll ans;

int main(){
    while(~scanf("%d", &n)){
        ans = 0;
        for(int i = 0; i < n; ++ i){
            scanf("%lld", a + i);
        }
        for(int i = n-1; i >= 0; -- i){
            if()
        }
    }
    return 0;
}